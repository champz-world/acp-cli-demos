interface:
  display_name: "ACP Paid Checkout"
  short_description: "ACP agent card subscription checkout"
  default_prompt: "Use $acp-paid-subscription-checkout to complete a bounded paid subscription checkout for my ACP agent and verify access."
policy:
  allow_implicit_invocation: true
